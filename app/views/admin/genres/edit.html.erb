<%= render 'layouts/admins_header' %>

<div class="container">
	<h1>ジャンル編集画面</h1>
	<br>
	<br>
	<p><%= notice %></p>
	<div class="row">
		<h4>
		<%= form_for Genre.find(params[:id]), :url => {:controller => :genres, :action => :update} do |f| %>
			ジャンル　
			<%= f.text_field :name %>
			<%= f.label :gender,:有効 %>
			<%= f.radio_button :valid_status,:active %>
			<%= f.label :gender,:無効 %>
			<%= f.radio_button :valid_status,:is_deleted %>
			<%= f.submit '変更を保存する' %>
		<% end %>
		</h4>
	</div>
</div>